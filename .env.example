# No environment variables are required by the public site at the moment.
# Add new vars here (with safe placeholder values) as they're introduced.

# Microsoft Clarity project ID (CLI-50). Optional: the site falls back to the
# live project ID baked into ClarityAnalytics.astro when this is unset. Override
# only to point at a different Clarity project.
# PUBLIC_CLARITY_PROJECT_ID=wdaa1y3eea
